server:
  port: 9010

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/carrito
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    web: trace
    org:
      springframework:
        web: trace